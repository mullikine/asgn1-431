#ifndef COMPARATORS_H_
#define COMPARATORS_H_

extern int posting_len_cmp(const void * a, const void * b);
extern int compare_ranks(const void * a, const void * b);
extern int compare_strings(const void * s1, const void * s2);

#endif
